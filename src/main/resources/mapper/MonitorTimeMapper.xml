<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.trs.gov.kpi.dao.MonitorTimeMapper">

    <select id="getMonitorStartTime" resultType="Date">
        SELECT max(startTime)
        FROM monitortime
        WHERE siteId = #{siteId}
        AND typeId = #{typeId}
    </select>

    <select id="getMonitorEndTime" resultType="Date">
        SELECT max(endTime)
        FROM monitortime
        WHERE siteId = #{siteId}
        AND typeId = #{typeId}
    </select>
</mapper>