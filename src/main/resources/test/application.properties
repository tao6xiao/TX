spring.datasource.url=jdbc:mysql://10.27.77.114:3306/kpidb_test?useUnicode=true&characterEncoding=UTF-8&useSSL=false&allowMultiQueries=true
spring.datasource.username=admin
spring.datasource.password=trs@mlf.028
spring.datasource.driver-class-name=com.mysql.jdbc.Driver

# Charset of HTTP requests and responses. Added to the "Content-Type" header if not set explicitly.
spring.http.encoding.charset=UTF-8
# Enable http encoding support.
spring.http.encoding.enabled=true
# Force the encoding to the configured charset on HTTP requests and responses.
spring.http.encoding.force=true

#mybatis的mapper文件路径
mybatis.mapper-locations=classpath:mapper/*.xml

#log4j配置
logging.config=classpath:log4j2.xml

# AOP
spring.aop.auto=true
spring.aop.proxy-target-class=true

# 外部模块
# 采编中心服务地址
service.outer.editcenter.url=http://govtest.dev3.trs.org.cn

# CKM校对地址
service.outer.ckm.url=http://jiaodui.trs.cn/rs/detect

# 问政互动的服务地址
service.outer.nbhd.url=http://govdev.dev3.trs.org.cn/NBHD

# 网脉的服务地址
service.outer.bas.url=http://tapi.trs.cn/bas